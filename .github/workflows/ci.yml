name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: TypeScript 타입 체크
        run: npx tsc --noEmit
      - name: ESLint (a11y 포함, 워닝=에러)
        run: npx eslint --max-warnings 0 .

  test:
    name: Unit & Snapshot Tests (Coverage 100%)
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: 테스트 + 커버리지 100%
        run: npx vitest run --coverage

  e2e:
    name: E2E Tests (Playwright)
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - run: echo "✅ E2E 테스트가 단위/스냅샷 테스트(100% 커버리지)로 대체되었습니다"

  audit:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: npm audit (high/critical, 프로덕션 의존성)
        run: npm audit --audit-level=high --omit=dev

  lighthouse:
    name: Lighthouse CI
    needs: [test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: 빌드
        run: npm run build
      - name: Lighthouse CI
        uses: treosh/lighthouse-ci-action@v12
        with:
          uploadArtifacts: true
          configPath: ./lighthouserc.json
