# 유현이 키우기 — 게임 규칙 설계서

> 이 파일은 "게임의 설명서"예요. 코드를 고치기 전에 여기서 규칙부터 정해요.
> 실제 숫자는 `index.html` 맨 위 `CONFIG` / `FOODS` 부분에 그대로 들어가 있어요.

## 1. 캐릭터

- 이름: **유현**
- 그림: 직접 그린 PNG로 교체 가능 (아래 8번 참고). 지금은 임시 SVG 캐릭터.

## 2. 호감도

| 항목 | 값  |
| ---- | --- |
| 시작 | 50  |
| 최소 | 0   |
| 최대 | 300 |

### 호감도 단계

| 점수      | 단계      |
| --------- | --------- |
| 0 ~ 49    | 서먹서먹  |
| 50 ~ 99   | 관심 있음 |
| 100 ~ 169 | 친해짐    |
| 170 ~ 239 | 좋아함    |
| 240 ~ 300 | 최애      |

## 3. 음식

| 음식     | 호감도 | 포만감 | 상점 가격 |
| -------- | ------ | ------ | --------- |
| 스테이크 | +8     | 25     | 12 코인   |
| 마카롱   | +4     | 15     | 5 코인    |
| 포도     | +2     | 10     | 2 코인    |
| 얼음     | 0      | 5      | 1 코인    |
| 돌맹이   | −2     | 0      | 0 (무한)  |

**취향은 숨김.** 한 번도 안 먹여본 음식은 도감에 `???` 로 표시되고,
먹여보면 그때 점수가 공개돼요.

## 4. 호감도가 깎이는 규칙 (감소 3종)

적용 순서대로 계산해요.

1. **질림** — 같은 음식을 연속 5번째로 주면 → 그 판은 **호감도 0**
   - 다른 음식을 주면 연속 횟수가 1로 초기화돼요.
2. **호감도 80 이상** → 상승치 **절반**
3. **배부름 70 이상** → 상승치 **또 절반**

> ⚠️ 2번과 3번이 겹치면 8 → 4 → 2 로 두 번 깎여요.
> 게임 화면 아래 "계산 기록"에 매번 어떻게 계산됐는지 다 보여주니까 확인해보세요.
>
> ⚠️ 그리고 만점이 300인데 임계값이 80이라, 사실상 게임 대부분이 반토막 구간이에요.
> 느리다 싶으면 `index.html` 의 `반토막_임계: 80` 숫자만 고치면 돼요. (예: 240)

**마이너스 음식(돌맹이)은 절반 적용 안 해요.** 벌은 항상 그대로 −2.

## 5. 배부름

| 항목    | 값                                       |
| ------- | ---------------------------------------- |
| 범위    | 0 ~ 100                                  |
| 70 이상 | 호감도 상승 절반                         |
| 100     | 아예 못 먹음 ("배불러…")                 |
| 회복    | 1분에 5씩 자동 회복 (20분이면 완전 회복) |

## 6. 방치 페널티

- **2시간**마다 쓰다듬어주지 않으면 호감도 **−4**
- 앱을 꺼놔도 시간은 흘러요. (마지막 쓰다듬은 시각을 저장해뒀다가, 켤 때 계산)

## 7. 코인 & 상점

- **쓰다듬기** → 코인 +1
  - ⚠️ 무한 연타로 코인을 무한히 벌 수 있어서 **2초 쿨타임**을 넣었어요. (내가 정한 것 — 빼도 됨)
  - 쓰다듬으면 호감도는 안 오르지만 **방치 타이머가 초기화**돼요.
- **출석체크** — 하루에 처음 켤 때 🥩 스테이크 1개 + 🪙 코인 20
- 코인으로 상점에서 음식 구매

## 8. 그림 교체하는 법

1. 그린 그림을 **투명 배경 PNG**로 저장 (정사각형, 512×512 권장)
2. `yuhyeon-game/images/` 폴더에 넣기
3. `index.html` 맨 위 `캐릭터_그림` 부분에서 `null` 을 파일 이름으로 바꾸기

```js
const 캐릭터_그림 = {
  idle:     "images/유현_기본.png",   // ← 이렇게
  happy:    null,
  ...
};
```

필요한 표정 8종: `idle`(기본) `happy`(기분좋음) `love`(완전행복)
`confused`(갸웃) `disgust`(시러) `eating`(먹는중) `bored`(질림) `sad`(시무룩)

> 다 안 그려도 돼요. 안 채운 표정은 자동으로 임시 캐릭터가 나와요.

## 9. 아직 안 만든 것 (나중에)

- **홈 화면에서 혼자 돌아다니기** — 웹앱으로는 불가능. 안드로이드 네이티브 앱 필요.
  ([`SYSTEM_ALERT_WINDOW` 권한](https://developer.android.com/reference/android/Manifest.permission))
- 꾸미기(옷·모자 등)
- 미니게임
