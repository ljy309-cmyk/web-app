FROM mcr.microsoft.com/devcontainers/python:3.11-bookworm

# Node.js는 devcontainer feature로 설치 (devcontainer.json 참고)
# NodeSource 저장소 직접 설정은 GPG 키 만료 등 문제가 빈번하므로
# 공식 devcontainer feature를 사용하는 것이 가장 안정적
